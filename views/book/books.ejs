<!DOCTYPE html> 
<html> 
    <head> 
        <title>Books</title>
        <link rel="stylesheet" href="../css/style.css"/>
    </head> 
    <body> 
        <h1>รายการหนังสือ</h1> 
    <table>
        <tr>
            <th>ลำดับ</th>
            <th>ชื่อหนังสือ</th>
            <th></th>
        </tr>
        <% for(var i=0; i < books.length; i++) { %>
        <tr>
            <td><%= books[i].book_id %></td>
            <td><%= books[i].book_name %></td>
            <td>
                <a href="http://localhost:5500/book/<%= books[i].book_id %>">View</a>
                <!-- <a href="http://localhost:5500/book/update/<%= books[i].book_id %>">Edit</a> -->
                <!-- <a href="http://localhost:5500/book/delete/<%= books[i].book_id %>">Delete</a> -->
            </td>
        </tr>
        <% } %>
    </table>
    <div class="link-center">
    <a href="http://localhost:5500/books/create">Create New Book</a>
    <a href="http://localhost:5500/">Back to List</a>
    </div>
</body>
</html>